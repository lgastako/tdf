module Data.Vector.Sized.X
  ( module X
  ) where

import Data.Vector.Sized as X

-- import qualified Data.Vector.Sized as SV

-- join :: Vector n (Vector m a) -> Vector m (Vector n a)
-- join = SV.concatMap f
--   where
--     f = undefined


-- join' = updateVec (Sized.join . fmap toVec)
